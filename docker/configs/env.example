# 데이터베이스 설정
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=mhp_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# PgAdmin 설정
PGADMIN_PASSWORD=admin

# 애플리케이션 설정
DEBUG=False
CORS_ORIGINS=*
ALLOWED_HOSTS=*

# 네트워크 설정 (외부 접근용)
# 서버 컴퓨터의 IP 주소를 입력하세요
# 예: HOST_IP=192.168.1.100
HOST_IP=0.0.0.0

# 보안 설정 (프로덕션에서는 특정 도메인만 허용)
# CORS_ORIGINS=http://192.168.1.101:3000,http://192.168.1.102:3000
# ALLOWED_HOSTS=192.168.1.100,localhost 